# AngularStandaloneapp
The task involves building an Angular app where users can upload Excel files, search data with filters, and download results as Excel. The upload screen will allow file selection and insert data into SQL Server. The search screen will filter results based on specific columns. Users can download filtered results in Excel format.
